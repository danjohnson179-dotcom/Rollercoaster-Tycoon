# Ride models

Place optimized GLB assets here. The simulator uses procedural geometry until a production model is available. Keep meshes under 100k triangles, use 1–2K PBR textures, and export distinct nodes for the arm, disc, seats, restraints, platform, and gates so the scene adapter can animate them independently.
